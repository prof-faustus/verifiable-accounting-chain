{
  "name": "@vaa/keys",
  "version": "0.1.0",
  "description": "Pillar 1: the PKI root and the deterministic general-ledger key hierarchy on the Bitcoin (BSV) curve.",
  "type": "module",
  "exports": { ".": { "types": "./dist/index.d.ts", "default": "./dist/index.js" } },
  "types": "./dist/index.d.ts",
  "scripts": { "build": "tsc -b", "test": "node --test \"test/**/*.test.ts\"", "lint": "tsc -b" },
  "dependencies": { "@vaa/bsv": "*" }
}

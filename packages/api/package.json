{
  "name": "@vaa/api",
  "version": "0.1.0",
  "description": "Enterprise service: anchor, prove, query, audit-path verify, chain append/verify, and bundle issue/verify, terminating in the BSV header chain.",
  "type": "module",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc -b",
    "test": "node --test \"test/**/*.test.ts\"",
    "lint": "tsc -b"
  },
  "dependencies": {
    "@vaa/bsv": "*",
    "@vaa/keys": "*",
    "@vaa/chain": "*",
    "@vaa/merkle": "*",
    "@vaa/proofstore": "*",
    "@vaa/evidence": "*",
    "@vaa/bundle": "*"
  }
}

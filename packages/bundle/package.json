{
  "name": "@vaa/bundle",
  "version": "0.1.0",
  "description": "Assembles and verifies the tiny auditor proof bundle: disclosed fields + Merkle path + chain evidence + inclusion proof.",
  "type": "module",
  "exports": { ".": { "types": "./dist/index.d.ts", "default": "./dist/index.js" } },
  "types": "./dist/index.d.ts",
  "scripts": { "build": "tsc -b", "test": "node --test \"test/**/*.test.ts\"", "lint": "tsc -b" },
  "dependencies": {
    "@vaa/bsv": "*",
    "@vaa/merkle": "*",
    "@vaa/proofstore": "*",
    "@vaa/evidence": "*",
    "@vaa/chain": "*",
    "@vaa/keys": "*"
  }
}

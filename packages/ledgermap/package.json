{
  "name": "@vaa/ledgermap",
  "version": "0.1.0",
  "description": "The field map (EP3420669B1): chart-of-accounts structure, field to path to key mapping, versioned mapping root, and per-field extraction across transactions.",
  "type": "module",
  "exports": { ".": { "types": "./dist/index.d.ts", "default": "./dist/index.js" } },
  "types": "./dist/index.d.ts",
  "scripts": { "build": "tsc -b", "test": "node --test \"test/**/*.test.ts\"", "lint": "tsc -b" },
  "dependencies": { "@vaa/bsv": "*", "@vaa/keys": "*", "@vaa/evidence": "*" }
}
